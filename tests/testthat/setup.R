# Test setup helper for scExploreR
#
# This file is automatically sourced by testthat before
# running tests. It loads common dependencies needed across
# test files.

# Load shiny for UI helper tests (tags, div, etc.)
library(shiny)
